import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Sistemas03ilcr', // Cambia "tuempresa" por tu nombre o dominio
  appName: 'S.O.S.GLOBAL',
  webDir: 'dist/public',  // Donde está el build de Vite
  bundledWebRuntime: false
};

export default config;